{
  "name": "stoneymountain-cmms",
  "version": "0.1.0",
  "description": "Maintenance management system for Stoneymountain Church",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "build": "npm install && npm run build:client",
    "build:client": "cd client-src && npm install && npm run build && rm -rf ../public/static ../public/*.js ../public/*.css ../public/manifest.json && cp -r build/* ../public/",
    "start": "node server.js",
    "db:setup": "node db/setup.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {}
}
